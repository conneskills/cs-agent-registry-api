# ── PostgreSQL (required for production) ──
# Neon serverless Postgres (free tier: 0.5 GB, scale to zero)
DATABASE_URL=postgresql+asyncpg://user:pass@ep-xxxxx.us-east-2.aws.neon.tech/registry?sslmode=require

# ── LiteLLM integration (auto-register public agents) ──
LITELLM_URL=https://litellm.conneskills.com
LITELLM_MASTER_KEY=sk-...

# ── Phoenix/OTEL tracing (optional, no-op if not set) ──
# OTEL_EXPORTER_OTLP_ENDPOINT=http://phoenix:4317
